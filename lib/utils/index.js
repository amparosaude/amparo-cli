const chalk = require('./chalk')
const generateMigrationName = require('./generateMigrationName')
const pathExists = require('./pathExists')
const updateFile = require('./updateFile')

module.exports = {
  chalk,
  generateMigrationName,
  pathExists,
  updateFile,
}